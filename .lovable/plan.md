## Goal
Apply the uploaded `findestichanges.patch` (two commits) to integrate the "Find Esti" infographic as the home page, move the shelter map to `/map`, and add a GitHub Pages deployment workflow.

## Steps

1. Copy `user-uploads://findestichanges.patch` to `/tmp/` and apply with `git apply --reject` (per the bundle's instructions, since `src/routeTree.gen.ts` will be regenerated by the dev server).

2. Verify the patch produced the expected files:
   - **New**: `src/components/FindEsti.tsx`, `src/routes/map.tsx`, `.github/workflows/deploy-pages.yml`, `vite.config.pages.ts`
   - **Modified**: `src/routes/index.tsx` (now renders `FindEsti`), `src/routes/__root.tsx` (Esti meta), `src/router.tsx` (adds `basepath: import.meta.env.BASE_URL`), `package.json` (adds `build:pages` script)

3. Let the TanStack Router Vite plugin regenerate `src/routeTree.gen.ts` automatically.

4. Confirm the preview loads the new home page and `/map` route works with default filters (Youth Shelter + Drop-In Centre).

## Notes
- No design or content changes beyond what's in the patch.
- GitHub Pages activation (Settings → Pages → Source = GitHub Actions) is a manual repo step the user must do; I'll remind them.
